<% if @question.errors.present? %>
    <% @question.errors.full_messages.each do |message| %>
        $('.question-errors').addClass('alert alert-danger').html('<%= j message %>');
    <% end %>
<% else %>
    $('.question').html('<%= j render @question %>');
    $('.edit-question-link').click(function(e) {
        e.preventDefault();
        $(this).hide();
        $('.edit-question').show();
    });
<% end %>